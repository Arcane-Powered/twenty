// Mirrors IS_ENTERPRISE_UNLOCKED on the server: enterprise features are enabled
// unconditionally, so the licensing screens are replaced by a simple notice.
export const IS_ENTERPRISE_UNLOCKED: boolean = true;
